Content packs are organized by source (`base` for shipped content, `user` for local additions)
and by content type (`units`, `statuses`, and `battles`).

The `base` pack is the authoritative source for shipped battle content.
